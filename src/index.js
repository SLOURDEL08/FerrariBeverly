import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App /> {/* Rend ton composant App ou le composant racine de ton application */}
  </React.StrictMode>,
  document.getElementById('root')
);
